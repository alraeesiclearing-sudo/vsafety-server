# ============================================================
# ملف المتغيرات البيئية - انسخه إلى .env وعدّل القيم
# ============================================================

# ===== قاعدة البيانات (مطلوب) =====
# صيغة MySQL: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=mysql://inspection_user:inspection_pass123@localhost:3306/inspection_db

# ===== الأمان (مطلوب - غيّر هذه القيمة!) =====
# يجب أن يكون طويلاً وعشوائياً (على الأقل 64 حرف)
JWT_SECRET=change_this_to_a_very_long_random_secret_key_minimum_64_characters_long

# ===== إعدادات الخادم =====
NODE_ENV=production
PORT=3000

# ===== إعدادات قاعدة البيانات (للـ docker-compose فقط) =====
DB_ROOT_PASSWORD=rootpassword123
DB_NAME=inspection_db
DB_USER=inspection_user
DB_PASSWORD=inspection_pass123

# ===== Manus OAuth (اختياري - للمصادقة بحساب Manus) =====
VITE_APP_ID=
OAUTH_SERVER_URL=
VITE_OAUTH_PORTAL_URL=
OWNER_OPEN_ID=
OWNER_NAME=

# ===== Manus Built-in APIs (اختياري) =====
BUILT_IN_FORGE_API_KEY=
BUILT_IN_FORGE_API_URL=
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
